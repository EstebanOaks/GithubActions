name: Test

on: [push]

jobs:
  hola:
    runs-on: ubuntu-latest

    steps:
      - name: Test Step
        run: echo "Este es un paso de prueba"
      
      - name: touch file
        run: touch hola.txt

      - name: set text
        run: echo "hola mundo" >> hola.txt

      - name: read file
        run: cat hola.txt

  other:
    runs-on: ubuntu-latest
    
    steps:
    - name: LS
      run: ls -al